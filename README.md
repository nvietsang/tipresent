# Threshold Implementation of PRESENT on ARM

This repository contains the ARM assembly code for a threshold implementation of the PRESENT cipher. By default, it is configured for power trace measurements using a ChipWhisperer Lite.